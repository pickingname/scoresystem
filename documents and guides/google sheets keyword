### google sheets keyword 

> keyword for user sorting and good things

---

#### access labels

> `gsheets` is access to google sheets, default access (can edit)
>
> `repl` the server hosting access (can changes, high access)
>
> `gforms` access to google forms (can changes)

---

### behavior labels

> `arch` anarchy, usually at the back of the class
>
> `aarch64` 64bit extension of arm architecure
>
> `l` applies at the lowest point
>
> `h` applies at the highest point
>
> `clsled` class leader, 2 people
>
> `s` strike system, very bad behavior on our record and this will happens

---